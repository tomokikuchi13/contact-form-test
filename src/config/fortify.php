<?php

use Laravel\Fortify\Features;

return [

    /*
    |--------------------------------------------------------------------------
    | Fortify Guard
    |--------------------------------------------------------------------------
    |
    | Fortify が認証に使用するガードを指定します。
    |
    */

    'guard' => 'web',

    /*
    |--------------------------------------------------------------------------
    | Fortify Password Broker
    |--------------------------------------------------------------------------
    |
    | パスワードリセット時に使用するブローカー。
    |
    */

    'passwords' => 'users',

    /*
    |--------------------------------------------------------------------------
    | Username / Email
    |--------------------------------------------------------------------------
    |
    | 認証に使用するフィールドを指定できます（デフォルトは email）。
    |
    */

    'username' => 'email',

    /*
    |--------------------------------------------------------------------------
    | Home URL
    |--------------------------------------------------------------------------
    |
    | ログイン後にリダイレクトされる URL
    |
    */

    'home' => '/dashboard',

    /*
    |--------------------------------------------------------------------------
    | Fortify Views
    |--------------------------------------------------------------------------
    |
    | Fortify で使用するビューをカスタマイズする場合に設定
    | ここにクロージャを定義して view('auth.register') などを返す
    |
    */

    'views' => true,

    /*
    |--------------------------------------------------------------------------
    | Features
    |--------------------------------------------------------------------------
    |
    | Fortify の機能を有効化/無効化
    |
    */

    'features' => [
        Features::registration(), // ユーザー登録
        Features::resetPasswords(), // パスワードリセット
        Features::emailVerification(), // メール認証
        Features::updateProfileInformation(), // プロフィール更新
        Features::updatePasswords(), // パスワード更新
        Features::twoFactorAuthentication(), // 2段階認証
    ],

    'home' => '/dashboard',
    
];
